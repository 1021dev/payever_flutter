import 'package:flutter/material.dart';

class SantanderInvoiceDE extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      padding: EdgeInsets.symmetric(vertical: 5),
      child: Container(color: Colors.black, child: Text("Santader invoice de")),
    );
  }
}
