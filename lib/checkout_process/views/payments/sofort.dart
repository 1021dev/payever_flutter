import 'package:flutter/material.dart';

class Sofort extends StatelessWidget {
  @override
  
  Widget build(BuildContext context) {
    return Container(
      color: Colors.black,
      child: Text("Sofort"),
    );
  }
}