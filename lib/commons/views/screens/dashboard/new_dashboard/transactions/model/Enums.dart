enum SortType {
  name,
  highTotal,
  lowTotal,
  date
}

enum FilterType {
  id,
  reference,
  date,
  payment_type,
  status,
  specific_status,
  channel,
  amount,
  total,
  currency,
  customer_name,
  customer_email,
  merchant_name,
  merchant_email,
  seller_name,
  seller_email
}